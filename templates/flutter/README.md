# {{SAFE_NAME}}

## Build Setup

1. Add build folder flutter (build/web/).

```bash
yarn
# or
yarn start
```

```js
({
  type: 'Flutter',
  toolsbuild: 'Webpack5',
  name: '{{SAFE_NAME}}',
  port: '{{PORT}}',
  framework: 'flutter',
  css: 'css',
})
```